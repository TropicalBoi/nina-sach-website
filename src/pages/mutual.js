import * as React from "react";
import Layout from "../components/layout";
import MutualComp from "../components/mutualComp";

const Mutual = () => {
  return (
    <Layout>
      <MutualComp />
    </Layout>
  );
};

export default Mutual;
