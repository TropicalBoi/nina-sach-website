import * as React from "react";
import { css } from "@emotion/css";

const spaceBar = () => {
  return (
    <div
      className={css`
        width: auto;
        height: 400vh;
        background-color: black;
      `}
    >
      
    </div>
  );
};

export default spaceBar;
